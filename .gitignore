# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.whl
src/claude_monitoring/_version.py

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project data (local only)
*.db
*.db-wal
*.db-shm
claude_watch_output/

# Test / coverage
.coverage
htmlcov/
coverage.xml
.pytest_cache/

# Build
*.so
MANIFEST

# Ruff
.ruff_cache/

# Claude — the entire .claude/ folder + CLAUDE.md are operator-local
# AI-workflow files, not part of the product. Untracked from the public
# repo but Claude Code reads them from disk unchanged.
.claude/
CLAUDE.md
CLAUDE.local.md
ai-runtime-monitor/

# Environment files
.env
*.env.local

# Node
node_modules/

# Playwright MCP cache
.playwright-mcp/

# Pitch decks and binary planning artifacts (kept locally, not in git)
*.pptx
*.pdf

# Local-only operational notes, never committed
docs/notes/
docs/internal/
# Brand-site copy lives canonically at ~/Documents/vigil-notes/brand-site-copy.md
# (local launch-prep notes). A working-tree copy at this path keeps getting
# swept in by `git add -A`; ignore it so it stops without forcing deletion.
docs/brand-site-copy.md

# Internal/operational docs — live in ~/Documents/vigil-notes, never in the repo.
# Re-asserting after the 2026-06 cleanup so accidental re-commit is blocked.
docs/TOOLING.md
docs/RUNBOOK.md
docs/AUDIT_*.md
docs/SPRINT_*.md
docs/EXECUTION_PLAYBOOK*.md
docs/RUFF_WARMUP.md
docs/architecture-baseline-suggestions.md
docs/ENTERPRISE_ROADMAP.md
docs/CONTROL_PLANE_*.md
docs/PROBE_DESIGN.md
docs/HOW_THE_CODE_WORKS.md
docs/CLAUDE-archive-*.md
docs/roadmap/
docs/CC_*.md
CC_PROMPT*.md
CC_DISPATCH*.md
control-plane/
