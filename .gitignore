# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.whl
src/claude_monitoring/_version.py

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project data (local only)
*.db
*.db-wal
*.db-shm
claude_watch_output/

# Test / coverage
.coverage
htmlcov/
coverage.xml
.pytest_cache/

# Build
*.so
MANIFEST

# Ruff
.ruff_cache/

# Claude
.claude/*
# But track checked-in agent definitions and rubrics — the project's
# review-intelligence configuration (Layer 8 in docs/SSDLC_ENFORCEMENT.md).
# Local state (settings.local.json, worktrees, scheduled_tasks.lock,
# etc.) stays ignored.
!.claude/agents/
!.claude/agents/**
!.claude/rubrics/
!.claude/rubrics/**
ai-runtime-monitor/

# Environment files
.env
*.env.local

# Node
node_modules/

# Playwright MCP cache
.playwright-mcp/

# Pitch decks and binary planning artifacts (kept locally, not in git)
*.pptx
*.pdf

# Local-only operational notes, never committed
docs/notes/
docs/internal/
